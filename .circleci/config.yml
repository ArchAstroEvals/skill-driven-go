version: 2.1
jobs:
  test:
    docker:
      - image: cimg/go:1.24
    steps:
      - checkout
      - run: gofmt -l .
      - run: go vet ./...
      - run: go test -count=1 ./...
workflows:
  ci:
    jobs:
      - test
